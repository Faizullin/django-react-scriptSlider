import * as React from 'react';
import { IScript } from '../../models/IScript';

export interface IScriptItemProps {
    script: IScript
}

export default function ScriptItem (props: IScriptItemProps) {
  return (
    <div>
      
    </div>
  );
}
